# The scipy-notebook image already includes pandas, numpy and scikit-learn.
# This file documents the versions the notebook relies on.
# If you run the notebook OUTSIDE Docker, install these with:
#   pip install -r requirements.txt
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
requests==2.32.3
